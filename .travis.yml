language: ruby
sudo: false

before_install:
  - gem update --system
  - gem install bundler -v 1.17.3

rvm:
  - 2.3
  - 2.4
  - 2.5
  - ruby-head

cache:
  bundler: true

install:
  - bundle install

script:
  - bundle exec rake
